# ОБРАЗЕЦ. Не применять в таком виде — значения заменить реальными.
# Для production: использовать Sealed Secrets / External Secrets Operator.
apiVersion: v1
kind: Secret
metadata:
  name: dsp-secrets
type: Opaque
stringData:
  SECRET_KEY: "replace-with-long-random"
  DATABASE_URL: "postgresql+psycopg2://dsp:change-me@dsp-db:5432/dsp"
  TOKEN_TTL_HOURS: "24"
